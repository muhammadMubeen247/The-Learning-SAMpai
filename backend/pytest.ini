[pytest]
asyncio_mode = auto
testpaths = app/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    llm: marks tests that make real LLM API calls (skip with -m "not llm")
    integration: marks tests that require external services (PostgreSQL, Neo4j, ChromaDB)
